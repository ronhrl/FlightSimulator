<UserControl x:Class="FlightSimulator2.view.SpeedRatio"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator2.view"
             mc:Ignorable="d" 
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBlockSpeedPlayBack" HorizontalAlignment="Center"  Text="Speed PlayBack:" TextWrapping="Wrap" VerticalAlignment="Center" Grid.Column="0"/>
        <TextBox x:Name="textBoxSpeedPlayBack" HorizontalAlignment="Left"  Text="{Binding VM_SpeedRatio, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"  VerticalAlignment="Center" Width="40" Height="20" Grid.Column="1" Margin="10,0,0,0"/>

    </Grid>
</UserControl>
